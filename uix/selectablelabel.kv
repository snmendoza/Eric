#:import hex kivy.utils.get_color_from_hex
#
<SelectableLabel>:
        canvas.before:
                Color:
                        rgba: hex('#33B5E5') if self.selected else (0, 0, 0, 0)
                Rectangle:
                        pos: self.pos
                        size: self.size
