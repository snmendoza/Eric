<SelectableLabel>:
        canvas.before:
                Color:
                        rgba: (.0, .9, .1, .3) if self.selected else (0, 0, 0, 1)
                Rectangle:
                        pos: self.pos
                        size: self.size
